/// Holds global values used throughout the app.
class Values {
  /// If this ctor is not added, the autocompleter will think this class can be
  /// initialized.
  Values._();

  /// The max width any layout can have (mainly used on tablets and desktop).
  static double maxWidth = 600;
}
